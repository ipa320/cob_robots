<?xml version="1.0"?>
<launch>

	<!-- args -->
	<arg name="robot" value="raw-mini"/>
	<arg name="pc1" default="localhost"/>
	<arg name="env-script" default="$(find cob_bringup)/env.sh"/>
	<arg name="sim" default="false"/>

	<!-- upload robot description -->
	<include file="$(find cob_hardware_config)/upload_robot.launch">
		<arg name="robot" value="$(arg robot)"/>
	</include>
	<!-- upload default configuration parameters -->
	<include file="$(find cob_default_robot_config)/upload_param.launch">
		<arg name="robot" value="$(arg robot)"/>
	</include>

	<group>
		<machine name="$(anon pc1)" address="localhost" 
		env-loader="$(arg env-script)" default="true" timeout="30"/>

		<!-- start hardware -->
		<group unless="$(arg sim)">
			<include file="$(find rplidar_ros)/launch/rplidar.launch"/>
		</group>

		<include file="$(find cob_bringup)/components/raw_mini_base.launch" >
			<arg name="robot" value="$(arg robot)" />
			<arg name="sim" value="$(arg sim)"/>
		</include>
		
		<!-- start additional packages -->
		<include file="$(find cob_bringup)/tools/teleop.launch">
			<arg name="robot" value="$(arg robot)"/>
		</include>
		<include file="$(find cob_bringup)/tools/base_collision_observer.launch">
			<arg name="robot" value="$(arg robot)"/>
		</include>
		<include file="$(find cob_bringup)/tools/robot_state_publisher.launch">
			<arg name="robot" value="$(arg robot)"/>
		</include>
		<include file="$(find cob_script_server)/launch/script_server.launch"/>
			<!-- simulation only -->
		<include if="$(arg sim)" file="$(find cob_bringup)/tools/fake_diagnostics.launch">
			<arg name="fake_diagnostics" value="'base_laser_front, base_laser_rear, -pc1, joy'"/>
		</include>
	</group>

	<machine name="$(anon pc1)" address="localhost" env-loader="$(arg env-script)" default="true" timeout="30"/>

</launch>
