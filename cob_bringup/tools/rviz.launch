<?xml version="1.0"?>
<launch>

        <!-- start rviz with configuration -->
        <node pkg="rviz" type="rviz" name="$(anon rviz)" args="-d $(find cob_hardware_config)/common/rviz_config.vcg" respawn="false" output="screen" />

</launch>

