<?xml version="1.0"?>
<launch>

	<arg name="arm" />

	<rosparam file="$(find cob_controller_configuration_gazebo)/controller/arm_controller_$(arg arm).yaml" command="load"/>

	<node name="arm_controller_spawner" pkg="controller_manager" type="spawner" args="arm_controller" />

</launch>
