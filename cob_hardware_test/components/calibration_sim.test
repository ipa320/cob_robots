<?xml version="1.0"?>
<launch>

    <machine name="local" address="localhost"/>
    <!-- start dialog service -->
    <node pkg="cob_hardware_test" type="dialog.py" name="dialog_server"/>
    <!-- launch robot -->
    <include file="$(find cob_bringup_sim)/launch/robot.launch" />
    <!-- open rviz with configuration -->
    <machine name="rviz_machine" address="localhost"/>
    <node machine="rviz_machine" pkg="rviz" type="rviz" name="$(anon rviz)" args="-d $(find cob_hardware_test)/config/calibration.vcg" respawn="false" output="screen" />
    <!-- include test -->
    <include file="$(find cob_hardware_test)/components/calibration.test.xml" />

</launch>
